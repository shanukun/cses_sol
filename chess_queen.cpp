#include <bits/stdc++.h>

using namespace std;

const int n = 8;
char mat[n][n];

int main() {
    int st = 0;
    for (int i = 0; i < n; ++i)
        for (int j = 0; j < n; j++)
            cin >> mat[i][j], st += mat[i][j] == '*';

    for (int q = 0; q < n; ++q) {
        for (int i = 0; i < n; ++i) {
            for (int j = 0; j < n; ++j) {
            }
        }
    }
}
